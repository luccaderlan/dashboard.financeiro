{
  "name": "meucontrole-api",
  "version": "1.0.0",
  "description": "Backend do MeuControle — Dashboard Financeiro Pessoal",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node --env-file=.env src/server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^10.0.2",
    "@fastify/helmet": "^13.0.1",
    "@fastify/jwt": "^9.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^6.7.0",
    "bcryptjs": "^3.0.2",
    "fastify": "^5.3.2",
    "jsonwebtoken": "^9.0.3",
    "pino-pretty": "^13.0.0",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
