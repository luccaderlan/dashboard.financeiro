# URL do backend (sem barra final)
# Em desenvolvimento: deixe vazio — o proxy do Vite (vite.config.js) encaminha
#   /auth e /user para localhost:3333 automaticamente. Veja .env.development.
# Em produção: URL completa da sua API no Railway/Render.
VITE_API_URL="https://sua-api.railway.app"
